/** Example decision categories — industry blueprints decide which exist. */
export const DECISION_CATEGORY_EXAMPLES = Object.freeze([
  Object.freeze({ id: "operational", label: "Operational" }),
  Object.freeze({ id: "strategic", label: "Strategic" }),
  Object.freeze({ id: "administrative", label: "Administrative" }),
  Object.freeze({ id: "compliance", label: "Compliance" }),
] as const);
