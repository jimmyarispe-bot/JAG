/**
 * Campus references for scheduling (metadata hints only).
 * Campus entity remains owned by org-structure; this documents scheduling keys.
 */

export const ACADEMY_SCHEDULING_CAMPUS_METADATA_KEYS = Object.freeze([
  "campusId",
  "schoolId",
  "displayName",
  "timezone",
  "modalitySupport",
  "status",
] as const);
