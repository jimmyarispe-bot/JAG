# Academy — Organization Studio

The Academy Way is described as **answers** (`describeAcademyOrganization`), then produced into an Organization Blueprint via JAG Organization Studio. Capability packs (`academy.core`) attach as Organization Blueprint cargo for the Runtime Generation Engine.

Transitional pack seeds live under `src/packages/academy/capability-packs/` until domains are fully synthesized from Industry + answers.
