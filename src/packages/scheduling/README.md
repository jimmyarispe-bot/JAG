# Scheduling Capability Pack (`scheduling.core`)

**Universal Organizational Scheduling** — fourth production Capability Pack.

Time coordination (who, what, when, resources) — not calendars, meeting providers, optimization, or attendance.

Declarative dependencies: `identity.core`, `documents.core`, `communications.core` (^1.0.0, required).

See `docs/jag-os/capability-packs/scheduling/`.
