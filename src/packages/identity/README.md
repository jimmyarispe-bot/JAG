# Identity Capability Pack (`identity.core`)

**Universal Organizational Identity** — first production Capability Pack.

Authored as a third-party pack: no platform privileges, no engine modifications.

Owns organizational identity models (organizations, people, membership, roles, groups, profiles, permission bindings). Does **not** own authentication or authorization execution.

See `docs/jag-os/capability-packs/identity/`.
