/**
 * ProcessModel — declarative process contribution.
 * Compatible with ProcessDefinition shape consumed by ProcessRegistry.
 */

import type { ProcessDefinition } from "@/jag/processes";

/** Process model is the Process Engine definition (declarative data). */
export type ProcessModel = ProcessDefinition;
