/**
 * JAG Identity — ownership namespace.
 * Prefer deep imports from `@/lib/platform/iam` / `@/lib/platform/identity` for specifics.
 */
export {
  getIdentityContext,
  type IdentityContext,
} from "@/lib/platform/identity/context";
