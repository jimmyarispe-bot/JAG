/** Marketplace Foundation v1. */
export const JAG_MARKETPLACE_VERSION = "1.0.0" as const;

/** Declared Platform compatibility baseline (no platform code change). */
export const JAG_MARKETPLACE_PLATFORM_VERSION = "1.0.0" as const;
